import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'

// (Optional) If you’re using Tailwind, also:
// import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
